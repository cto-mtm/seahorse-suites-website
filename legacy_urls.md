# Legacy URL Redirects

Tracker for 301 redirects configured in `www/nuxt.config.ts` → `routeRules`.
Keep both in sync.

| Old URL | New URL | Notes |
| ------- | ------- | ----- |
| `/rooms` | `/#suites` | Example: old "Rooms" page merged into homepage Suites section |
